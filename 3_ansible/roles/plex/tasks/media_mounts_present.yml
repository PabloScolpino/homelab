---
- name: CIFS Utilities present
  become: true
  ansible.builtin.package:
    name: "{{ item }}"
    state: present
  loop:
    - cifs-utils
    - linux-modules-extra-{{ ansible_kernel }}

- name: Mount points exist
  become: true
  ansible.builtin.file:
    path: "{{ item.target }}"
    state: directory
    mode: '0755'
    owner: root
    group: root
  loop: "{{ media_shares }}"

- name: Network shares mounted
  become: true
  ansible.posix.mount:
    src: "{{ item.source }}"
    path: "{{ item.target }}"
    fstype: cifs
    opts: 'vers=3.0,username={{ username }},password={{ password }},iocharset=utf8'
    state: mounted
  loop: "{{ media_shares }}"
